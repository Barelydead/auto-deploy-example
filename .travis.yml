language: php

php:
    - "7.1"

sudo: false

git:
    submodules: false

# before_install:
#     - openssl aes-256-cbc -K $encrypted_1ab7f14560ba_key -iv $encrypted_1ab7f14560ba_iv -in deploy/deploy_rsa.enc -out /tmp/deploy_rsa -d
#     - eval "$(ssh-agent -s)"
#     - chmod 600 /tmp/deploy_rsa
#     - ssh-add /tmp/deploy_rsa

before_script:
    - make install
    - make prepare

deploy:
    provider: script
    script: ./vendor/deployer/dist/dep depoly -vvv


# addons:
#     apt:
#         packages:
#             #- php-codesniffer
#             #- phpmd
#             #- shellcheck
#
#
#
# matrix:
#     allow_failures:
#         #- php: "7.0"
#         #- php: "7.1"
#         - php: nightly
#         - php: hhvm
#
#
#
# before_script:
#     - make install
#
#
# script:
#     - make check
#     - make test



# notifications:
#     irc: "irc.freenode.org#dbwebb"
#     urls:
#       - https://webhooks.gitter.im/e/6e4660b0024de67de0af
#     on_success: change  # options: [always|never|change] default: always
#     on_failure: always  # options: [always|never|change] default: always
#     on_start: never     # options: [always|never|change] default: always
